
<div class="container">
  <%= simple_form_for ([@cocktail, @dose]) do |f| %>
    <%= f.input :description %>
    <%= f.input :ingredient, collection: Ingredient.name, prompt: 'Choose your ingredient' %>
    <%= f.button :submit %>
  <% end %>
</div>

